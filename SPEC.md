# Pulse 仕様書

## これは何か

PC での操作（打鍵・クリック）を数え、**途切れずに続いた回数をコンボとして**
キーに出す。手が止まると落ちる。Atom の activate-power-mode のコンボ表示を、
画面の外に出したもの。

## なぜ作るか

power-mode はエディタの中でしか効かず、しかも画面を汚す。粒子と画面揺れが邪魔で切る人が多い。

- **エディタの中だけでなく、PC の全作業に効く**（ターミナルでも、ブラウザでも、資料作成でも）
- **画面を汚さない。** 盤面の外に出すので、作業の視界に一切入らない
- 既存に無い。Marketplace の Counter 系は「Stream Deck のキー自身を押した回数」を数えるもので、
  OS 全体の入力を数えるものは見当たらなかった（2026-09-15 時点）

## 配布しない

**自分用と割り切る。** 全キー入力を読むプラグインは審査が厳しいはずで、
利用者にも入力監視の権限を要求することになる。Marketplace には出さない。

## 数えるもの・数えないもの

**数えるのは件数だけ。どのキーが押されたかは読み取らないし、記録もしない。**
これは自分用でも守る。ヘルパが外に出すのは以下の3種類の件数のみ。

| 記号 | 対象 |
| --- | --- |
| k | キー押下（`keyDown`） |
| m | マウスのボタン押下（左・右） |

**スクロールは数えない。** トラックパッドの2本指スクロールは慣性で毎秒何十件も飛び、
1回の操作として打鍵と釣り合わない。マスクから外してイベント自体を来なくする。

## 構成

```
tap-probe（Swift・常駐）  … CGEventTap で件数だけを数え、1行ずつ標準出力に流す
      ↓ stdout
plugin.mjs（Node・常駐）  … 集計・コンボ判定・SVG描画・記録
      ↓ WebSocket
Stream Deck
```

- ヘルパは **listenOnly** のタップ。イベントは素通しする（入力を横取りしない）
- ヘルパが落ちたらプラグイン側が起動し直す
- Node 側は依存パッケージゼロ。Swift 側も標準フレームワークのみ

## 画面（72×72）

```
┌────────────┐
│   COMBO     │
│    47       │  ← 現在のコンボ数（大きく）
│ ▓▓▓▓▓░░░░   │  ← 切れるまでの残り時間
└────────────┘
```

- 色はレベルで変える。10 / 25 / 50 / 100 / 200 / 400 で段階的に
- コンボが切れた瞬間は、**数字を一度だけ大きく出してから 0 に落とす**（切れたことが分かるように）

## コンボの規則

- 入力が1件あるたびに +1
- **3秒（設定可）入力が無ければ 0 に戻る**
- 打鍵とクリックを区別せず、同じ1件として数える
  （区別すると「クリック連打で稼げる」等の歪みを考える羽目になり、目的と関係ない）

## 描画レート（実測に基づく制約）

実測で **Stream Deck アプリの CPU は fps に比例し、1fps あたり 0.2% 強**。
デバイス側の描画能力は 120fps でも頭打ちにならないので、**上限を決めるのは CPU だけ**。

| 状態 | レート | CPU（実測値からの見積） |
| --- | --- | --- |
| 手が止まっている | **送らない** | 0% |
| 打っている間 | 20〜30fps | 約6% |
| コンボの節目（レベルが上がった瞬間） | 60fps を 0.3秒だけ | 一瞬 12% |

**「手が止まったら送らない」が最重要。** 常駐プラグインで常時12%は許容できない。
コンボは打っている間しか動かないので、この制約と噛み合う。

## 押した時

短押しで表示を順に切替える。

1. 現在のコンボ
2. 今日の最高コンボ
3. 今日の総操作数
4. 直近60秒の操作数（APM）

長押し（1.5秒）は**何もしない**。記録のリセットは設定画面からのみ。
手が触れただけで今日の記録が消えるのは事故になる。

## 権限が無い時

キーに「入力監視の許可が要る」と出し、**押すとシステム設定の該当ページを直接開く**。

```
open "x-apple.systempreferences:com.apple.preference.security?Privacy_ListenEvent"
```

見えている状態と、それを解決する操作が同じキーに乗る。ここだけは押す意味がある。

## 実測で分かっている落とし穴

- **`CGEvent.tapCreate` は権限が無くても成功する。** 戻り値で判定してはいけない。
  判定は `CGPreflightListenEventAccess()` で行う
- 権限は**ヘルパのバイナリ自身に付く**。Stream Deck 側に許可を出す必要は無い。
  ただしバイナリを再ビルドすると許可し直しになる可能性があるので、要確認
- **Secure Input**（パスワード欄など）が有効な間は打鍵が取れないはず。
  その間コンボが不自然に切れる。実装前に確かめる

## 記録

- 今日の最高コンボ、通算最高、日ごとの総操作数を端末内に保存する
- **どこにも送信しない**

## 受け入れ条件

1. 打鍵・クリックのいずれでもコンボが伸びる
1b. トラックパッドの2本指スクロールではコンボが伸びない
2. 3秒放置で 0 に戻り、戻る瞬間が見て分かる
3. 手を止めている間、Stream Deck アプリの CPU が平常時（1%未満）に戻る
4. 権限が無い状態で起動すると、その旨が出て、押すと設定画面が開く
5. ヘルパを強制終了しても、プラグインが起動し直して復帰する
6. Stream Deck を再起動しても今日の記録が残る

## 作る順

| 段階 | 内容 |
| --- | --- |
| 1 | ヘルパを常駐版にし、件数を1行ずつ流す |
| 2 | プラグイン側で受けてコンボ判定・数字だけ描く |
| 3 | 残り時間ゲージとレベルの色 |
| 4 | 描画レートの制御（止まったら送らない） |
| 5 | 表示切替と記録 |
| 6 | 権限が無い時の案内と設定画面を開く導線 |
