# 実測メモ

測定環境: macOS 26.6.2 / Stream Deck 7.5.1 / Stream Deck 5×3 / Node 24.7 / Apple Silicon
測定日: 2026-09-15

## 1. 入力監視（CGEventTap）の権限

**結論: Stream Deck から起動した子プロセスでも、キーボード・マウス・スクロールを全部拾える。**

- Stream Deck 起動下で 120 秒計測し、**k=185 / m=28 / s=175** を取得
- **権限はヘルパのバイナリ自身に付く。** 先にターミナルから直接実行して許可を出したところ、
  その後 Stream Deck から起動した場合も `CGPreflightListenEventAccess()` が真を返した。
  Elgato Stream Deck.app 側に許可を出す必要は無かった
- **`CGEvent.tapCreate` は権限が無くても成功する。** 戻り値で権限を判定してはいけない。
  イベントが1件も来ないだけで、エラーにならない。判定は `CGPreflightListenEventAccess()` で行う
- 未確認: Secure Input（パスワード欄など）が有効な間は打鍵が取れないはず。要確認

## 2. `setImage` の送信レート

キー1枚に、回転する点を描いた SVG を送り続けた結果（2回測って同じ）。

| 目標 | 実送信 | WebSocketのバッファ最大 |
| --- | --- | --- |
| 10fps | 9.8 | 681 B |
| 20fps | 19.3 | 681 B |
| 30fps | 29.0 | 681 B |
| 60fps | 57.6 | 681 B |
| 120fps | 107.9 | 685 B |

**バッファが一度も溜まらない。** 681バイトは1通分なので、Stream Deck は送った分を
その場で受け取り切っている。**送信側は律速にならない。**
120fps で 107 止まりなのは Node のタイマー刻みの限界で、Stream Deck の詰まりではない。

**目視の結果: 10fps から 120fps まで、上げるほど滑らかになり続けた。**
デバイスの描画は頭打ちにならない。つまり **fps の上限を決めるのは描画能力ではなく CPU だけ**。

## 3. CPU 負荷（Stream Deck アプリ本体）

| 状態 | Stream Deck | プラグイン(node) |
| --- | --- | --- |
| 平常時 | 0.6% | 0.2% |
| 30fps 送信中 | 約 6% | 約 2% |
| 60fps 送信中 | 約 12% | 約 3% |
| 120fps 送信中 | 約 26% | 約 5% |

**おおむね fps に比例し、1fps あたり 0.2% 強。** 描画コストは Stream Deck アプリ側に乗る。

### 設計上の結論

**常時 60fps は選べない。** 常駐プラグインで 12% は重すぎる。
**入力がある瞬間だけ跳ね上げ、手が止まったら止める**のが正しい。

### そのあと分かったこと（2026-09-16）

Marketplace のプラグインガイドラインに、**キーの絵の更新は毎秒10回を超えてはならない**
という明文の規定があった。上の実測から導いた「打鍵中だけ30fps」は、CPU の面では
妥当でも規定に反する。

そこで設計を次のように変えた。

- 送信そのものに関門を置き、**最小間隔 100ms を機械的に守らせる**
- 送れない間に来た要求は捨てず、**最新の1件だけ**を覚えて間隔が空いたら送る。
  こうすると速く打っても最後の1打は必ず画面に出る
- 演出は連続的な補間をやめ、**コマ送り**にした（輪は4コマ、切れは3段階）。
  100ms 刻みで滑らかに見せようとすると、かえって中途半端になる

10fps なら CPU は約2%。上の表から見て、常駐する道具として妥当な位置に収まる。
